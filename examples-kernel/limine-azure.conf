timeout: 0
serial: yes
verbose: yes

# Azure-specific limine config for the unified kernel example.
# DHCP only — Azure has a production-grade DHCP server.
# Built into kernel.vhd via `cmake --build build --target kernel-vhd`.

/embclox Kernel Example (Azure, DHCP)
    protocol: limine
    kernel_path: boot():/boot/kernel
    cmdline: net=dhcp
