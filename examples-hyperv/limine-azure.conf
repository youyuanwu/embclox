timeout: 0
serial: yes
verbose: yes

# Azure-specific limine config: DHCP only, no menu.
# Azure has a production-grade DHCP server so DHCP works reliably.
# Built into hyperv.vhd via `cmake --build build --target hyperv-vhd`.

/embclox Hyper-V Example (Azure, DHCP)
    protocol: limine
    kernel_path: boot():/boot/kernel
    cmdline: net=dhcp
