timeout: 0
serial: yes
verbose: yes

# QEMU SLIRP CI uses static IP 10.0.2.15 baked into the kernel; no
# cmdline parsing needed for the e1000 example today.

/embclox e1000 Example
    protocol: limine
    kernel_path: boot():/boot/kernel
